import { TimeLapsePipe } from './time-lapse.pipe';

describe('TimeLapsePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeLapsePipe();
    expect(pipe).toBeTruthy();
  });
});
